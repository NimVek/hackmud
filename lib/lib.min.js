function() {
    // Convert hackmud timestr to date object
    function e(e) {
        var r = e.replace(/(\d{2})(\d{2})(\d{2})\.(\d{2})(\d{2})/, "20$1-$2-$3T$4:$5-04:00");
        return new Date(r);
    }
    // Export the symbols
    return {
        parse_timestr: e
    };
}
